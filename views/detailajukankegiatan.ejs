<!DOCTYPE html>
<html lang="id" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail Kegiatan</title>
    <link rel="stylesheet" href="/stylesheets/output.css" />
    <script src="/preline.js"></script>
</head>
<body class="bg-gray-50 flex flex-col h-full">
    <header class="flex flex-wrap sm:justify-start sm:flex-nowrap z-50 w-full bg-white text-sm py-4 dark:bg-gray-800">
        <nav class="max-w-[85rem] w-full mx-auto px-4 sm:flex sm:items-center sm:justify-between" aria-label="Global">
            <a class="flex-none text-xl font-semibold dark:text-white" href="#">Sahabat Relawan</a>
        </nav>
    </header>

    <main class="w-full max-w-[85rem] py-10 px-4 sm:px-6 lg:px-8 mx-auto">
        <div class="mb-5">
            <h1 class="text-2xl font-bold text-gray-800 dark:text-white">Detail Kegiatan</h1>
        </div>

        <div class="bg-white border border-gray-200 rounded-xl shadow-sm dark:bg-slate-900 dark:border-gray-700">
            <div class="p-4 sm:p-7">
                <div class="space-y-4">
                    <h2 class="text-xl font-bold text-gray-800 dark:text-white"><%= kegiatan.judul %></h2>
                    <% if (kegiatan.gambar) { %>
                        <div style="display: flex; justify-content: center;">
                            <img src="/uploads/<%= kegiatan.gambar %>" alt="Gambar Kegiatan" 
                                 style="max-width: 100%; height: auto; border-radius: 0.5rem; object-fit: cover; max-height: 384px; width: auto;">
                        </div>
                    <% } %>
                    <p class="text-gray-600 dark:text-gray-400"><%= kegiatan.deskripsi %></p>
                    <p><strong>NPSN:</strong> <%= kegiatan.npsn %></p>
                    <p><strong>Nama Sekolah:</strong> <%= kegiatan.namaSekolah %></p>
                    <p><strong>Lokasi:</strong> <%= kegiatan.lokasi %></p>
                    <p><strong>Kuota Relawan:</strong> <%= kegiatan.kuotaRelawan %></p>
                    <p><strong>Mulai:</strong> <%= moment(kegiatan.mulai).format('DD MMMM YYYY') %></p>
                    <p><strong>Selesai:</strong> <%= moment(kegiatan.selesai).format('DD MMMM YYYY') %></p>
                    <p><strong>Status:</strong> <%= kegiatan.status %></p>
                
                    <% if (kegiatan.dokumen) { %>
                    <div>
                        <a href="/uploads/<%= kegiatan.dokumen %>" class="text-blue-500 hover:text-blue-700" target="_blank">Lihat Dokumen</a>
                    </div>
                    <% } %>
                </div>
            </div>
        </div>

        <div class="mt-5">
            <a href="/kegiatan" class="py-3 px-4 inline-flex justify-center items-center gap-2 rounded-md border border-transparent font-semibold bg-blue-500 text-white hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-all text-sm dark:focus:ring-offset-gray-800">
                Kembali ke Daftar Kegiatan
            </a>
        </div>
    </main>
</body>
</html>